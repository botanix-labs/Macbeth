# Runs unit tests.

name: unit

on:
  pull_request:
  merge_group:
  push:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  SEED: rustethereumethereumrust

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  test:
    name: test / ethereum (${{ matrix.partition }}/2)
    runs-on: ubuntu-latest
    env:
      RUST_BACKTRACE: 1
    strategy:
      matrix:
        partition: [1, 2]
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - uses: taiki-e/install-action@nextest
      - name: Install Protobuf Compiler
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - name: Start BTC Server
        run: |
          cd bin/btc-server
          cargo run --bin btc-server -- --btc-network regtest \
          --identifier 0 --address 0.0.0.0:8080 \
          --db "./db1" --min-signers 2 --max-signers 2 \
          --toml ./config.toml  --fee-rate-diff-percentage 30 \
          --bitcoind-url http://localhost:18443 --bitcoind-user \
          foo --bitcoind-pass bar \
          --jwt-secret "bin/btc-server/jwt.hex" \
          --fall-back-fee-rate-sat-per-vbyte 5 &
      - name: Run tests
        run: |
          cargo nextest run \
            --locked --features "ethereum" \
            --workspace --exclude examples --exclude ef-tests \
            --partition hash:${{ matrix.partition }}/2 \
            -E "kind(lib) | kind(bin) | kind(proc-macro)"

  state:
    name: Ethereum state tests
    runs-on: ubuntu-latest
      # group: Reth
    env:
      RUST_LOG: info,sync=error
      RUST_BACKTRACE: 1
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Checkout ethereum/tests
        uses: actions/checkout@v4
        with:
          repository: ethereum/tests
          path: testing/ef-tests/ethereum-tests
          submodules: recursive
          fetch-depth: 1
      - uses: dtolnay/rust-toolchain@stable
      - uses: taiki-e/install-action@nextest
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - name: Install Protobuf Compiler
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - name: Start BTC Server
        run: |
          cd bin/btc-server
          cargo run --bin btc-server -- --btc-network regtest \
          --identifier 0 --address 0.0.0.0:8080 \
          --db "./db1" --min-signers 2 --max-signers 2 \
          --toml ./config.toml  --fee-rate-diff-percentage 30 \
          --bitcoind-url http://localhost:18443 --bitcoind-user \
          foo --bitcoind-pass bar \
          --jwt-secret "bin/btc-server/jwt.hex" \
          --fall-back-fee-rate-sat-per-vbyte 5 &
      - run: cargo nextest run --release -p ef-tests --features ef-tests

  doc:
    name: doc tests (ethereum)
    runs-on: ubuntu-latest
    env:
      RUST_BACKTRACE: 1
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - name: Run doctests
        run: cargo test --doc --workspace --features "ethereum"

  unit-success:
    name: unit success
    runs-on: ubuntu-latest
    if: always()
    needs: [test, state, doc]
    timeout-minutes: 30
    steps:
      - name: Decide whether the needed jobs succeeded or failed
        uses: re-actors/alls-green@release/v1
        with:
          jobs: ${{ toJSON(needs) }}
